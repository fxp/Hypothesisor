{
  "name": "walkthru-builder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Offline walkthrough builder for Walkthru — no dependencies, plain Node.",
  "scripts": {
    "build": "node build.mjs"
  }
}
